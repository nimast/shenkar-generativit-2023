function setup() {
    createCanvas(600, 600);
    background(103, 110, 207);
//noLoop();
}
function draw() {
    var circleColorStroke = color(255, 252, 5);
    stroke(circleColorStroke);
    var circleColor = color(103, 110, 207);
    fill(circleColor);
    circle(200, 200, 900);
    noStroke();
    var circleColor = color(235, 218, 31);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(600 + second(), 400, 250);
    var circleColor = color(137, 187, 210);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(500 - second(), 111, 200);
    var circleColor = color(92, 96, 158);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(356 - second(), 40, 118);
    var circleColor = color(106, 143, 195);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(440 + second(), 245, 70);
    var circleColor = color(212, 220, 92);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(440 - second(), 230, 140);
    var circleColor = color(142, 148, 227);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(199 + second(), 99, 229);
    var circleColor = color(114, 146, 143);
    fill(circleColor);
    // for (let i = 0; i < 100; i++) {
    // 	circle(random(200, 200), random(500, 100), random(70, 500));
    // }
    circle(400 - second(), 100, 77);
    var circleColor = color(140, 157, 95);
    fill(circleColor);
    // for (let i = 0; i < 100; i++) {
    // 	circle(random(200, 200), random(500, 100), random(70, 500));
    // }
    circle(290 + second(), 350, 200);
    var circleColor = color(146, 193, 176);
    fill(circleColor);
    // for (let i = 0; i < 100; i++) {
    // 	circle(random(200, 200), random(500, 100), random(70, 500));
    // }
    circle(88 - second(), 99, 100);
    var circleColor = color(144, 183, 146);
    fill(circleColor);
    // for (let i = 0; i < 100; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(544 + second, 111, 250);
    circle(378 + second(), 299, 160);
    var circleColor = color(163, 232, 176);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(27 - second(), 156, 177);
    var circleColor = color(112, 122, 249);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(15 + second(), 389, 260);
    circleColor = color(251, 191, 9);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(98 + second(), 400, 133);
    circleColor = color(153, 141, 73);
    fill(circleColor);
    // for (let i = 0; i < 100; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(109 + second(), 17, 156);
    circleColor = color(198, 175, 42);
    fill(circleColor);
    // for (let i = 0; i < 100; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(240 + second(), 230, 122);
    circleColor = color(179, 164, 79);
    fill(circleColor);
    // for (let i = 0; i < 100; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(50 + second(), 370, 50);
    circleColor = color(157, 205, 209);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(165 - second(), 320, 150);
    circleColor = color(274, 143, 58);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(240 + second(), 380, 68);
    circleColor = color(119, 162, 142);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(178 - second(), 415, 23);
    circleColor = color(174, 72, 139);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(512 - second(), 340, 97);
    circleColor = color(243, 137, 105);
    fill(circleColor);
    // for (let i = 0; i < 5; i++) {
    // 	circle(random(200, 600), random(40, 400), random(70, 250));
    // }
    circle(199 + second(), 66, 100);
    circle(520 + second(), 111, 60);
    circle(450 - second(), 380, 110);
    squareColore = color(73, 96, 2);
    noStroke();
    fill(squareColore);
    rect(0, height / 1.4, width, height);
    var grassColors = [
        color(135, 196, 62),
        color(138, 209, 88),
        color(81, 103, 47),
        color(85, 123, 6)
    ];
    var numLines = 20; // Number of grass lines
    var grassSegmentWidth = width / numLines;
    var grassSegmentHeight = 169;
    for(let y = 1; y < grassSegmentHeight; y += 11)for(let x = 0; x < width; x += grassSegmentWidth){
        var grassColor = random(grassColors);
        fill(grassColor);
        beginShape();
        vertex(x, height - y);
        vertex(x + grassSegmentWidth / 2, height - y - 5);
        vertex(x + grassSegmentWidth, height - y);
        endShape(CLOSE);
    }
}

//# sourceMappingURL=sara.fe083bc0.js.map
